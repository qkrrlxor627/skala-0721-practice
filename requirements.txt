# 실습 3 실행에 필요한 라이브러리
# 설치: pip install -r requirements.txt

pandas==3.0.3      # EDA, IQR 이상치 제거, named aggregation 집계
polars==1.42.1     # Lazy API 집계 (scan_csv → ... → collect)
duckdb==1.5.4      # SQL GROUP BY 집계
pyarrow==25.0.0    # Polars 결과를 Pandas DataFrame 으로 변환할 때 사용

# 테스트 도구
pytest==9.1.1      # 단위 테스트
pytest-cov==7.1.0  # 커버리지 측정 (기준 80%)
