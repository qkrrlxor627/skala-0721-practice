# SKALA 파이썬 실습 (실습 3 · 실습 4)

같은 매출 데이터(`sales_100k.csv`, 100만 행)를 두 실습에 걸쳐 다룹니다.

| 실습 | 주제 | 제출 파일 | 요구사항 |
|---|---|---|---|
| **3** | Pandas EDA · Polars Lazy · DuckDB SQL 비교 | [`실습3/광주_1반_박기택.py`](실습3/광주_1반_박기택.py) | [`실습3/requirement.md`](실습3/requirement.md) |
| **4** | 시각화 4종 · 통계 검정 · sklearn Pipeline | [`실습4/광주_1반_박기택.py`](실습4/광주_1반_박기택.py) | [`실습4/requirement2.md`](실습4/requirement2.md) |

- 실습 3 결과 보고서: [`실습3/result.md`](실습3/result.md) *(실행할 때마다 실측값으로 다시 만들어집니다)*
- 코드 작성 규칙: [`Docs/ref.md`](Docs/ref.md) *(두 실습에서 정착시킨 형식)*

**실습 4는 실습 3의 정제 결과를 이어받습니다.** 같은 IQR 처리로 1,000,000행 → 973,806행을 만든 뒤, 그 데이터로 시각화·검정·모델 학습을 수행합니다.

---

## 1. 폴더 구조

```
실습3/   광주_1반_박기택.py · requirement.md · resultEx.md · result.md · 실행결과.txt
실습4/   광주_1반_박기택.py · requirement2.md · eda_2x2.png
         sales_model.joblib · sales_by_region_category.html · 실행결과.txt
Docs/    ref.md                      ← 두 실습 공통 코드 작성 규칙
tests/   conftest.py · test_practice3.py · test_practice4.py
sales_100k.csv · run_all.sh · requirements.txt · pyproject.toml
```

실습별로 제출 파일·요구사항·산출물을 한 폴더에 모았습니다. 두 실습의 제출 파일명이 `캠퍼스명_반_이름.py`로 같기 때문에 폴더로 구분해야 합니다. 원본 CSV는 두 실습이 함께 쓰므로 루트에 둡니다.

각 스크립트는 CSV를 **자기 폴더 → 상위 폴더** 순으로 찾으므로, 채점 시 `.py`와 CSV를 같은 폴더에 두어도 그대로 동작합니다.

---

## 2. 실행 방법

```bash
# 1) 가상환경 만들기 (Python 3.11 기준)
python3.11 -m venv .venv
source .venv/bin/activate

# 2) 라이브러리 설치
pip install -r requirements.txt

# 3) 실행
python 실습3/광주_1반_박기택.py      # 실습 3 (약 15초)
python 실습4/광주_1반_박기택.py      # 실습 4 (약 7초)

# 화면 출력을 파일로 남기려면
python 실습3/광주_1반_박기택.py > 실습3/실행결과.txt
```

> 원본 데이터 `sales_100k.csv`(77MB)는 용량이 커서 저장소에 포함하지 않았습니다.
> 실습 자료에서 내려받아 저장소 루트에 두고 실행해 주세요.
> 실습 4 스크립트는 자기 폴더와 상위 폴더를 차례로 찾으므로, 채점 시 `.py`와 CSV를 같은 폴더에 두어도 동작합니다.

## 3. 테스트

```bash
pytest
```

테스트 94건, 커버리지 95%입니다. 80% 미만이면 자동으로 실패하도록 `pyproject.toml`에 설정해 두었습니다.
원본(100만 행) 대신 손으로 계산해 검증할 수 있는 소형 표본(실습 3은 12행, 실습 4는 300행)을 사용해 10초 안에 끝납니다.

---

## 4. 출력 구성

실행하면 **결론부터** 보입니다. 근거가 되는 상세 수치는 그 뒤에 붙습니다.

```
[1/5] 데이터 읽기 완료 — 1,000,000건          ← 진행 상황 (한 줄씩)
 ...
[5/5] 실행 시간 측정 중 — 세 도구 × 10회

========================================================
 핵심 결과                                     ← 여기부터 결론
========================================================
 [1] 세 도구 집계 결과   일치 (81개 그룹)
 [2] 가장 빠른 도구      Polars — Pandas 대비 14.80배
 [3] 이상치 제거         1,000,000행 → 973,806행 (2.6%)
 [4] 매출 1위 그룹       서울·의류 749.9억원 (30,054건)

[매출 상위 10개 그룹]
순위 region  category         총매출        평균      건수
----------------------------------------------------------
1    서울    의류          749.9억원   249.5만원    30,054
...

------------------- 이하 상세 -------------------          ← 근거
[상세 1] 기초 탐색 — df.info() / isnull().sum() / describe()
[상세 2] IQR 이상치 제거 (Q1·Q3·상하한)
[상세 3] 세 도구 결과 동일성
[상세 4] 실행 시간 표
[상세 5] 자체 검증
```

금액은 억·만 단위로 바꾸고 한글 폭을 고려해 표를 정렬했습니다. `to_string()`을 그대로 쓰면 `7.499275e+10` 같은 지수 표기가 나와 규모가 한눈에 들어오지 않기 때문입니다.

---

## 5. 산출물

| 파일 | 설명 |
|---|---|
| `실습3/광주_1반_박기택.py` | **실습 3 제출 본체.** EDA → IQR 이상치 제거 → 세 도구 집계 → 결과 비교 → 성능 측정 |
| `실습3/result.md` | 실습 3 결과 보고서 (자동 생성) |
| `실습4/광주_1반_박기택.py` | **실습 4 제출 본체.** 정제 → 차트 4종 → 통계 검정 → Pipeline → 저장 |
| `실습4/eda_2x2.png` | 2×2 서브플롯 차트 4종 (자동 생성) |
| `실습4/sales_model.joblib` | 학습된 Pipeline — 필수 산출물 (자동 생성) |
| `실습4/sales_by_region_category.html` | Plotly 인터랙티브 차트 — 필수 산출물 (자동 생성) |
| `tests/` | 단위 테스트 94건 (커버리지 95%) |
| `실습3/실행결과.txt`, `실습4/실행결과.txt` | 화면 출력 기록 |
| `실습3/실행로그.log`, `실습4/실행로그.log` | 시각·심각도가 함께 남는 실행 기록 (자동 생성) |

---

## 6. 데이터 (`sales_100k.csv`)

| 항목 | 값 |
|---|---:|
| 행 수 | 1,000,000행 |
| 열 수 | 11열 |
| `region` 결측 | 10,000건 |
| `category` 결측 | 8,000건 |
| `amount` 결측 | 5,000건 |

파일 맨 앞에 BOM(눈에 보이지 않는 표식)이 붙어 있습니다.

### 정제 시 제외되는 26,194건의 내역

두 실습 모두 `1,000,000행 → 973,806행`으로 정제합니다. 제외되는 26,194건은 **성격이 다른 두 가지**입니다.

| 사유 | 건수 | 성격 |
|---|---:|---|
| `amount` 결측 | 5,000건 | 금액을 아예 알 수 없어 집계 불가. **이상치가 아님** |
| IQR 범위 밖 | 21,194건 | 금액이 상한을 넘는 고액 거래. **이쪽이 이상치** |

합쳐서 "26,194건의 이상치를 제거했다"고 적으면 결측 5,000건까지 이상치로 잘못 전달되므로, 출력과 보고서에서 항상 나눠서 표기합니다.

참고로 이 IQR 이상치의 정체를 확인해보니, 무작위 잡음이 아니라 **`amount`가 `quantity × unit_price`의 8~15배로 부풀려진 오염 행**이었습니다(전체의 2.0%, 19,874건).

---

## 7. 세 도구의 결과를 일치시키기 위해 맞춘 것

문법만 옮겨 적으면 세 도구의 결과는 **반드시 달라집니다.** 실제로 확인한 원인은 세 가지였고, 각각 이렇게 해결했습니다.

| 원인 | 무엇이 달랐나 | 해결 |
|---|---|---|
| **결측 그룹** | Pandas `groupby`는 결측(NaN) 그룹을 기본으로 빼버리는 반면, Polars·DuckDB는 하나의 그룹으로 남긴다. 결측 지역 1만 건·카테고리 8천 건이 통째로 갈렸다. | 세 도구 모두 `미상`/`미분류`로 채운 뒤 집계 |
| **분위수 보간** | Polars `quantile()` 기본값은 `nearest`, Pandas·DuckDB는 `linear`. 각자 계산하면 IQR 경계가 어긋나 걸러지는 행이 달라진다. | IQR 경계를 Pandas로 **한 번만** 계산해 세 도구에 같은 숫자로 전달 |
| **동점 정렬** | `total`만으로 정렬하면 값이 같은 행의 순서가 도구마다 달라진다. | 정렬 기준을 `total 내림차순 → region → category`까지 통일 |

추가로 Pandas는 `encoding="utf-8-sig"`를 지정해야 BOM 때문에 첫 컬럼명이 깨지지 않습니다. Polars·DuckDB는 자동으로 처리합니다.

---

## 8. 실행 결과 요약

100만 행 기준, `timeit number=10` (세 도구 동일, 예열 1회 후 측정, CSV 읽기 포함):

| 순위 | 도구 | 1회 평균 | Pandas 대비 |
|---:|---|---:|---:|
| 1 | Polars Lazy | 약 0.039초 | 약 14배 |
| 2 | DuckDB SQL | 약 0.078초 | 약 7배 |
| 3 | Pandas | 약 0.564초 | 기준 |

세 도구의 집계 결과는 **모두 동일**했습니다. 가장 최근 실행의 정확한 수치는 [`실습3/result.md`](실습3/result.md)에 있습니다.

### 반복 횟수를 10회로 정한 근거

가장 빠른 Polars는 1회가 0.04초로 짧아 잡음에 취약합니다. 20회 표본으로 'Pandas 대비 배수'의 흔들림을 재봤습니다.

| N | Polars 배수 흔들림 | 비고 |
|---:|---|---|
| 1 | ±20% | 결론이 뒤집힐 정도 |
| 3 | ±9% | 요구사항 최소치 |
| **10** | **±3.5%** | 채택 (총 측정 약 7초) |
| 20 | ±2% 수준 | 시간 2배, 개선폭 미미 |

또한 **예열이 반복 횟수보다 중요했습니다.** Polars의 첫 실행은 이후 실행보다 **약 68% 느립니다**(0.066초 → 0.039초). 파일 캐시가 비어 있고 엔진 초기화가 함께 일어나기 때문인데, 이는 집계 성능이 아닙니다. `number=3`이면 이 한 번이 측정값의 1/3을 오염시킵니다.

실제로 별도 프로세스로 3회 실행한 결과 배수는 13.71 / 14.43 / 15.20 (±5%)였습니다. 위 표의 ±3.5%보다 조금 넓은데, 표본이 한 세션 안에서 얻은 값이라 프로세스마다 달라지는 초기화 비용과 시스템 부하까지는 반영하지 못했기 때문입니다. `number=3`일 때의 10.59 / 13.23 / 13.91 (±14%)과 비교하면 개선은 분명합니다.

---

## 9. 실습 3 감점 항목 대응

| 감점 항목 | 대응 | 회귀 테스트 |
|---|---|---|
| IQR 공식 오류 | `lower = Q1 - 1.5×IQR`, `upper = Q3 + 1.5×IQR`, `between()`으로 양끝 포함 판정 | `test_compute_iqr_bounds_손계산과_일치` |
| named aggregation 미사용 | `total=('amount','sum')` 형식으로 컬럼명 직접 지정 | `test_aggregate_pandas_컬럼명이_named_aggregation_형식` |
| Polars Eager 사용 | `scan_csv` 사용, `read_csv` 미사용 | `test_aggregate_polars_scan_csv_사용` |
| `collect()` 누락 | 체인 끝에서 호출, 반환 타입으로 검증 | `test_aggregate_polars_collect_호출됨` |
| `timeit` 반복 횟수 미통일 | 상수 `TIMEIT_NUMBER` **하나만** 참조 — 구조적으로 어긋날 수 없음 | `test_benchmark_세_도구가_같은_반복횟수` |

---

# 실습 4 · 시각화 · 통계 검정 · sklearn Pipeline

## 10. 실행 결과 요약

| 항목 | 결과 | 판정 |
|---|---|---|
| t-test (서울 vs 부산) | t=0.727, p=0.4673, d=0.003 | **차이 미확인** |
| 카이제곱 (region×category) | χ²=81.83, p=0.0659, V=0.0032 | **연관성 미확인** |
| 회귀 (LinearRegression) | R²=0.8411, MAE 60.1만원 | — |
| 회귀 (HistGradientBoosting) | R²=0.9786, MAE 5.2만원 | — |

### 검정 결과를 어떻게 읽어야 하는가

두 검정 모두 귀무가설을 기각하지 못했습니다. **이것은 "차이가 없다"를 증명한 것이 아닙니다.**

`p >= 0.05`가 뜻하는 것은 *차이·연관이 있다는 충분한 증거를 얻지 못했다*이지, *차이·연관이 없다*가 아닙니다. 통계 검정은 귀무가설을 기각할 수는 있어도 채택할 수는 없습니다. 그래서 출력 문구를 이렇게 적었습니다.

```text
t-test  : 차이가 있다는 증거를 찾지 못함 (평균이 같음을 증명한 것은 아님)
카이제곱 : 연관성이 있다는 증거를 찾지 못함 (독립임을 증명한 것은 아님)
```

**그렇다면 실무적으로는 뭐라고 말할 수 있나** — 여기서 효과크기가 필요합니다. 지역별 평균 매출은 2,461,793~2,476,501원으로 차이가 0.6% 미만이고, 표본이 973,806건으로 충분히 큰데도 잡히지 않았으며, 효과크기(Cohen's d=0.003, Cramér's V=0.0032)도 무시할 수준입니다. **이 셋을 근거로 삼아야** "실무적으로 의미 있는 차이는 없다"고 말할 수 있습니다. p-value 하나만으로는 그 말을 할 수 없습니다.

반대 방향의 함정도 있습니다. 표본이 97만 건이면 실무적으로 의미 없는 차이도 '통계적으로 유의'해질 수 있습니다. 그래서 p-value와 효과크기를 항상 함께 냅니다.

### 두 모델의 R² 차이가 뜻하는 것

이 데이터의 `amount`는 `quantity × unit_price`로 결정되는 **곱셈** 관계입니다. 선형 모델은 가중합만 표현할 수 있어 곱을 직접 만들지 못하는데도 R²가 0.84까지 나옵니다. 평균 주위의 1차 근사가 잘 통하기 때문입니다.

```
q×p ≈ p̄·q + q̄·p - q̄·p̄  +  (q-q̄)(p-p̄)
      └─ 선형으로 표현 가능 ─┘   └─ 표현 불가 ─┘
```

남는 교호작용 항이 전체 분산의 약 14%를 차지하고, 이것이 **선형 모델이 원리적으로 넘을 수 없는 한계선**(이론상 최대 R² ≈ 0.86)입니다. 트리 모델은 구간을 나눠 이 교호작용을 학습해 0.9786로 그 벽을 넘습니다.

**전처리는 완전히 동일하고 마지막 단계만 다릅니다.** 이것이 Pipeline으로 묶어 두는 이유입니다.

### 이 결과의 한계 — 예측 모델로 읽으면 안 됩니다

R²=0.98을 "매출을 잘 맞히는 모델"로 읽으면 틀립니다. **예측 대상 `amount`가 입력 변수 `quantity × unit_price`로 이미 결정되는 값**이라, 모델에 정답의 재료를 그대로 넣어 준 셈이기 때문입니다.

여기서 확인한 것은 예측력이 아니라 두 가지입니다.

1. 비선형 모델이 곱셈 관계를 선형 모델보다 잘 학습한다
2. 전처리를 Pipeline으로 묶어 두면 모델만 갈아끼워 이런 비교를 할 수 있다

실습의 목적(Pipeline 구성)에는 적절하지만, 실무 예측 과제라면 **주문 시점에 아직 알 수 없는 값**(예: 다음 달 수요)을 대상으로 삼고 `quantity`·`unit_price` 같은 사후 정보는 입력에서 빼야 합니다.

### 상위 10개 순위도 규모와 객단가를 구분해서

총매출 상위는 **거래 건수가 많은 지역**이 올라온 결과입니다. 상위 10개 그룹의 평균 객단가는 244.9~249.5만원으로 서로 비슷합니다. "서울이 잘 팔린다"는 규모의 이야기이지, "서울 고객이 더 비싸게 산다"는 뜻이 아닙니다.

---

## 11. 차트에서 발견한 것 — 2월 매출 급감은 계절성이 아니다

월별 총매출 차트만 보면 2월마다 매출이 뚝 떨어져 계절성처럼 보입니다. 하지만 그 달의 일수로 나누면 사라집니다.

| 기준 | 변동계수 |
|---|---:|
| 총매출 | 2.97% |
| **일평균 (일수 보정)** | **0.73%** |

총매출이 가장 낮은 3개월은 2023-02(28일), 2024-02(29일), 2024-11(30일) — 전부 짧은 달입니다. 장사가 안 된 게 아니라 **영업일이 짧았을 뿐**입니다.

그래서 월별 차트에 일평균 선을 함께 그렸습니다. 이때 보조 축의 범위를 주 축과 **같은 비율로 맞추는 것이 중요합니다.** 그냥 두면 matplotlib이 각 축을 자기 자료 범위에 꽉 채워 자동 확대하는 바람에, 0.7%밖에 안 움직이는 일평균 선이 3% 움직이는 총매출 선과 똑같이 출렁여 보여 정반대로 읽히게 됩니다.

---

## 12. 실습 4 감점 항목 대응

| 감점 항목 | 대응 | 회귀 테스트 |
|---|---|---|
| 2×2 서브플롯 미사용 | `plt.subplots(2, 2)` 하나의 Figure, 개별 `plt.show()` 없음 | `test_2x2_서브플롯을_사용` |
| p-value 해석 없음 | `ALPHA` 상수 기준 판정 + 해석문을 실측값에 따라 분기 생성 | `test_ttest_결과에_판정과_해석이_포함` |
| Pipeline 미사용 | `ColumnTransformer` + 모델을 `Pipeline` 객체 하나로 | `test_전처리와_모델이_하나의_Pipeline` |
| 모델 저장 없음 | `joblib.dump()` → 재로딩 → **예측값 일치까지 확인** | `test_모델이_joblib_파일로_저장되고_재로딩됨` |
| HTML 저장 없음 | `fig.write_html()`, `fig.show()` 미사용 | `test_plotly는_show가_아니라_write_html_사용` |

> 소스 코드를 문자열로 검사하는 테스트는 **AST로 주석·docstring을 걷어낸 뒤** 확인합니다.
> "plt.show()를 쓰지 않는다"는 주석 때문에 '쓰고 있다'고 잘못 판정되는 것을 막기 위해서입니다.
